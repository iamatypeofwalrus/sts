package main

import "github.com/urfave/cli"

// TODO: stream in input and bin the data, plus add some functionality to configure bin-ing. prepping for a histogram!
func bin(c *cli.Context) error {
	return nil
}
